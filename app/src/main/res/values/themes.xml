<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.FrontendCodingTes" parent="Theme.Material3.Light.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="colorSurfaceVariant">@color/colorForm</item>
        <item name="colorOnSurfaceInverse">@color/colorForm</item>
    </style>

    <style name="Theme.FrontendCodingTes" parent="Base.Theme.FrontendCodingTes" />

    <style name="ShapeAppearanceOverlay.App.CornerSize50Percent" parent="">
        <item name="cornerFamily">rounded</item>
        <item name="cornerSize">50%</item>
    </style>

    <style name="ShapeAppearanceOverlay.App.Image" parent="">
        <item name="cornerFamily">rounded</item>
        <item name="cornerSize">10dp</item>
    </style>

    <color name="black">#000000</color>
</resources>